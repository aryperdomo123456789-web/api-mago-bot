# Branch `infra/producao`

Branch dedicada ao Compose, Nginx, bancos, Redis, deploy, backup, observabilidade e hardening do servidor `107.150.35.226`.

Segredos reais ficam fora do Git. Os arquivos versionados são apenas exemplos e configurações sem credenciais.
